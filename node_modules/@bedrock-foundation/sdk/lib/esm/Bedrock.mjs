import { BedrockCore } from './modules/BedrockCore.mjs';
export class Bedrock {
    constructor(basePath) {
        this.basePath = basePath !== null && basePath !== void 0 ? basePath : 'https://pay.bedrock.fyi';
        /**
         * modules
         */
        this.core = new BedrockCore({ basePath: this.basePath });
    }
}
//# sourceMappingURL=Bedrock.js.map