export const encodeError = (message, error) => {
    error.message = message;
    return error;
};
export const hasErrors = (errors) => errors.length > 0;
export const validate = (schema, params) => {
    var _a;
    const validator = schema.validate(params);
    const { value, error } = validator;
    return {
        value,
        errors: (_a = error === null || error === void 0 ? void 0 : error.details) !== null && _a !== void 0 ? _a : [],
    };
};
export const errorsToMessage = (errors) => errors.map((error) => error.message).join('\n');
//# sourceMappingURL=JoiUtil.js.map