import { Bedrock } from '@bedrock-foundation/sdk';
declare type UseNonceSocketConfig<T> = {
    bedrock: Bedrock;
    nonce: string | null;
    onChange?: (data: T) => void;
    onError?: (error: Error) => void;
};
declare type UseNonceSocket<T> = {
    data: T | null;
    error: Error | null;
};
export declare function useNonceSocket<T>(config: UseNonceSocketConfig<T>): UseNonceSocket<T>;
export {};
